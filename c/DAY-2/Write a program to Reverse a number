#include <stdio.h>
int main(){
    unsigned long int num, rev=0, a;
    printf("Enter a number: ");
    scanf("%lu", &num);
    a=num;
    while(a>0){
        rev=rev*10 + a%10;
        a=a/10;
    }
    printf("The reverse of %lu is %lu", num, rev);
    return 0;
}