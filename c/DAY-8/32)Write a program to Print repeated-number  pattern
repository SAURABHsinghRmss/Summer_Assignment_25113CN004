#include <stdio.h>
int main(){
    int i, j, h, a;
    printf("Enter the height of the triangle: ");
    scanf("%d", &h);
    for(i = 1; i <= h; i++){
        a=i;
        for(j = 1; j <= i; j++){
            printf("%d", a);
        }
        printf("\n");
    }
    return 0;
}