#include <stdio.h>
int main(){
    int i, j, h;
    printf("Enter the height of the triangle: ");
    scanf("%d", &h);
    for(i = 1; i <= h; i++){
        for(j = 1; j <= i; j++){
            printf("%c", 'A' + j - 1);
        }
        printf("\n");
    }
}